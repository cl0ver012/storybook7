export type Colors = {
  primary: string
  secondary: string
  primaryBackground: string
  secondaryBackground: string
  primaryBorderColor: string
  secondaryText: string
}

export type Fonts = {
  primary: string
  secondary: string
}
